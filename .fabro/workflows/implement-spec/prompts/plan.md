Read `docs/spec.md` and inspect the repository.

Write `docs/plan.md` as a short, ordered Markdown checklist using `- [ ]` for
each step. Every step must be independently implementable and verifiable, and
together the steps must fully satisfy the specification.

Do not implement the plan.
