Read `docs/spec.md` and `docs/plan.md`.

If the workflow context contains non-empty `validation_feedback`, address that
feedback. Otherwise, implement the first unchecked plan step.

Make the smallest complete change needed. Run focused checks when useful, but
do not work ahead.

Do not mark plan steps complete; validation owns the checklist.
